spring:
 profiles:
  active: local

---
spring:
  profiles: local

custom:
  title: "local title"

---
spring:
  profiles: prod

custom:
  title: "prod title"
---
logging:
  config: classpath:log4j2.xml

#WAS server setting
server:  port: 8000
  servlet:    context-path: /

# View Resolver setting
spring:  mvc:    view:      prefix: /WEB-INF/view/
      suffix: .jsp
      
# mongoDB setting  data:    mongodb:      uri: mongodb://localhost:27017
      database: admin
      username: root
      password: 1q2w3e!@#