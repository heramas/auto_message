spring:
 profiles:
  active: local

---
spring:
  profiles: local

custom:
  title: "local title"

---
spring:
  profiles: prod

custom:
  title: "prod title"
---
logging:
  config: classpath:log4j2.xml

#WAS server setting
server:  port: 8000
  servlet:    context-path: /product

# View Resolver setting
spring:  mvc:    view:      prefix: /WEB-INF/view/
      suffix: .jsp